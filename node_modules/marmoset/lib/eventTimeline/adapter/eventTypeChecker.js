"use strict";

Object.defineProperty(exports, "__esModule", {
    value: true
});
var arraylike = function arraylike(obj) {
    return obj && typeof obj === "object" && typeof obj.length === "number";
};

var checkEventType = function checkEventType(event, checks) {
    for (var i = 0; i < checks.length; i++) {
        var ctxs = checks[i].ctxs;
        var type = checks[i].type;
        if (checkItem(event, ctxs, type)) {
            return true;
        }
    }
    return false;
};

var checkItem = function checkItem(event, ctxs, type) {
    if (typeof type === "string") {
        if (type !== event.type) {
            return false;
        }
    } else if (arraylike(type)) {
        for (var j = 0; j < type.length; j++) {
            if (type[j] === event.type) {
                break;
            }
        }
        if (j === type.length) {
            return false;
        }
    } else {
        if (!type.test(event.type)) {
            return false;
        }
    }
    if (ctxs) {
        for (var _j = 0; _j < ctxs.length; _j++) {
            if (ctxs[_j] === event.ctx) {
                return true;
            }
        }
    } else {
        // default is all
        return true;
    }
};

exports["default"] = {
    checkEventType: checkEventType
};
module.exports = exports["default"];